{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/user_interaction_service/package.json",
                "localRoot": "${workspaceFolder}/user_interaction_service"
            }
        }
    ]
}